```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>kintsugi* · стратегии адаптации · неделя 2</title>
    <style>
        /* дизайн-система kintsugi* */
        :root {
            --bg-vanilla: #f1eada;
            --text-mahogany: #584738;
            --accent-gold: #c9a96e;
            --border-soft: rgba(88, 71, 56, 0.15);
            --bg-card: rgba(88, 71, 56, 0.03);
        }

        /* сброс стилей и принудительный lowercase */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            text-transform: lowercase;
        }

        body {
            background-color: var(--bg-vanilla);
            color: var(--text-mahogany);
            line-height: 1.8;
            padding: 40px 24px;
            display: flex;
            justify-content: center;
            align-items: flex-start;
            min-height: 100vh;
        }

        /* просторный вертикальный контейнер без жесткого узкого ограничения */
        .poster-container {
            width: 100%;
            max-width: 760px; /* расширили ширину, чтобы текст свободно дышал и не был узким */
            background-color: var(--bg-vanilla);
            border: 1px solid var(--border-soft);
            padding: 60px 48px;
            display: flex;
            flex-direction: column;
            gap: 50px;
            position: relative;
            box-shadow: 0 12px 40px rgba(88, 71, 56, 0.05);
            border-radius: 4px;
        }

        @media (max-width: 600px) {
            body {
                padding: 16px 8px;
            }
            .poster-container {
                padding: 40px 20px;
                gap: 40px;
            }
        }

        /* шапка */
        header {
            border-bottom: 1px solid var(--border-soft);
            padding-bottom: 30px;
            text-align: center;
        }

        .brand-meta {
            font-size: 0.85rem;
            letter-spacing: 0.2em;
            opacity: 0.7;
            margin-bottom: 12px;
        }

        h1 {
            font-size: 2.2rem;
            font-weight: 300;
            letter-spacing: -0.02em;
            line-height: 1.2;
            color: var(--text-mahogany);
            margin-bottom: 8px;
        }

        .subtitle {
            font-size: 1.1rem;
            letter-spacing: 0.05em;
            opacity: 0.8;
        }

        /* манифест */
        .manifesto-box {
            text-align: center;
            max-width: 600px;
            margin: 0 auto;
        }

        .manifesto-lead {
            font-size: 1.25rem;
            font-weight: 400;
            line-height: 1.6;
            margin-bottom: 12px;
        }

        .manifesto-sub {
            font-size: 0.95rem;
            opacity: 0.65;
        }

        /* интерактивная схема реакции */
        .reaction-flow {
            background: var(--bg-card);
            border: 1px solid var(--border-soft);
            border-radius: 6px;
            padding: 30px;
        }

        .flow-title {
            font-size: 0.9rem;
            letter-spacing: 0.1em;
            opacity: 0.8;
            margin-bottom: 24px;
            text-align: center;
            border-bottom: 1px solid var(--border-soft);
            padding-bottom: 10px;
        }

        .flow-grid {
            display: grid;
            grid-template-columns: 1fr auto 1fr auto 1fr;
            align-items: center;
            gap: 16px;
            text-align: center;
        }

        @media (max-width: 600px) {
            .flow-grid {
                grid-template-columns: 1fr;
                gap: 24px;
            }
            .flow-arrow {
                transform: rotate(90deg);
                margin: 8px 0;
            }
        }

        .flow-card {
            padding: 16px;
            border-radius: 4px;
            background: rgba(255, 255, 255, 0.4);
            border: 1px solid rgba(88, 71, 56, 0.05);
        }

        .flow-step {
            font-size: 0.75rem;
            letter-spacing: 0.1em;
            opacity: 0.5;
            margin-bottom: 8px;
        }

        .flow-name {
            font-size: 1rem;
            font-weight: 500;
            margin-bottom: 6px;
        }

        .flow-desc {
            font-size: 0.85rem;
            opacity: 0.7;
            line-height: 1.4;
        }

        .flow-arrow {
            font-size: 1.5rem;
            color: var(--accent-gold);
            opacity: 0.8;
            user-select: none;
        }

        /* 4 стратегии */
        .strategies-section {
            display: flex;
            flex-direction: column;
            gap: 24px;
        }

        .section-label {
            font-size: 0.9rem;
            letter-spacing: 0.15em;
            opacity: 0.8;
            border-bottom: 1px solid var(--border-soft);
            padding-bottom: 12px;
            margin-bottom: 8px;
        }

        .strategy-row {
            display: grid;
            grid-template-columns: 140px 1fr;
            gap: 24px;
            padding: 20px 0;
            border-bottom: 1px dashed var(--border-soft);
        }

        @media (max-width: 600px) {
            .strategy-row {
                grid-template-columns: 1fr;
                gap: 12px;
                padding: 16px 0;
            }
        }

        .strategy-title-box {
            display: flex;
            flex-direction: column;
            gap: 4px;
        }

        .strategy-title {
            font-size: 1.2rem;
            font-weight: 500;
            letter-spacing: 0.05em;
        }

        .strategy-meta {
            font-size: 0.75rem;
            color: var(--accent-gold);
            letter-spacing: 0.1em;
        }

        .strategy-details {
            display: flex;
            flex-direction: column;
            gap: 12px;
        }

        .detail-item {
            font-size: 0.95rem;
        }

        .detail-item strong {
            font-weight: 500;
            opacity: 0.7;
            display: inline-block;
            width: 75px;
        }

        /* цена стратегии */
        .price-box {
            background: rgba(199, 169, 110, 0.08);
            border-left: 2px solid var(--accent-gold);
            padding: 24px 30px;
            border-radius: 0 6px 6px 0;
        }

        .price-title {
            font-size: 1rem;
            letter-spacing: 0.1em;
            margin-bottom: 12px;
            font-weight: 500;
        }

        .price-list {
            list-style: none;
            display: flex;
            flex-direction: column;
            gap: 8px;
            font-size: 0.95rem;
            opacity: 0.85;
        }

        .price-list li {
            position: relative;
            padding-left: 16px;
        }

        .price-list li::before {
            content: "·";
            position: absolute;
            left: 0;
            color: var(--accent-gold);
            font-weight: bold;
        }

        /* вопросы для наблюдения */
        .questions-box {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .question-item {
            display: flex;
            gap: 20px;
            align-items: flex-start;
        }

        .question-num {
            font-size: 0.9rem;
            font-weight: 500;
            color: var(--accent-gold);
            letter-spacing: 0.1em;
            padding-top: 3px;
        }

        .question-text {
            font-size: 1rem;
            opacity: 0.9;
        }

        /* практика недели */
        .practice-container {
            border: 1px solid var(--border-soft);
            border-radius: 6px;
            padding: 30px;
            text-align: center;
            background: var(--bg-card);
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 20px;
        }

        .practice-title {
            font-size: 1.1rem;
            letter-spacing: 0.1em;
            font-weight: 400;
        }

        .practice-focus {
            font-size: 0.85rem;
            opacity: 0.6;
            letter-spacing: 0.05em;
        }

        /* стильный плеер */
        .audio-player {
            width: 100%;
            max-width: 480px;
            background: white;
            border: 1px solid var(--border-soft);
            padding: 16px 24px;
            border-radius: 40px;
            display: flex;
            align-items: center;
            gap: 20px;
            margin-top: 8px;
        }

        .play-btn {
            width: 44px;
            height: 44px;
            border-radius: 50%;
            background-color: var(--text-mahogany);
            border: none;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: background-color 0.2s ease;
        }

        .play-btn:hover {
            background-color: var(--accent-gold);
        }

        .play-icon {
            width: 0;
            height: 0;
            border-style: solid;
            border-width: 8px 0 8px 14px;
            border-color: transparent transparent transparent var(--bg-vanilla);
            margin-left: 4px;
            transition: all 0.2s ease;
        }

        .play-btn.playing .play-icon {
            border-style: double;
            border-width: 0px 0 0px 12px;
            border-color: transparent transparent transparent var(--bg-vanilla);
            margin-left: 0;
        }

        .progress-container {
            flex-grow: 1;
            height: 4px;
            background: rgba(88, 71, 56, 0.1);
            border-radius: 2px;
            position: relative;
            cursor: pointer;
        }

        .progress-bar {
            height: 100%;
            width: 0%;
            background: var(--accent-gold);
            border-radius: 2px;
            transition: width 0.1s linear;
        }

        .time-display {
            font-size: 0.8rem;
            opacity: 0.6;
            min-width: 35px;
            font-variant-numeric: tabular-nums;
        }

        /* подвал */
        footer {
            border-top: 1px solid var(--border-soft);
            padding-top: 35px;
            text-align: center;
            display: flex;
            flex-direction: column;
            gap: 12px;
        }

        .footer-quote {
            font-size: 1.15rem;
            letter-spacing: 0.05em;
            font-weight: 300;
        }

        .footer-meta {
            font-size: 0.8rem;
            letter-spacing: 0.15em;
            opacity: 0.5;
        }
    </style>
</head>
<body>

    <div class="poster-container">
        <!-- шапка -->
        <header>
            <div class="brand-meta">виктория голдберг · kintsugi* · неделя 2</div>
            <h1>стратегии адаптации</h1>
            <div class="subtitle">бей · беги · замри · угодничество</div>
        </header>

        <!-- манифест -->
        <section class="manifesto-box">
            <p class="manifesto-lead">тело выбирает защиту мгновенно.</p>
            <p class="manifesto-sub">это не черта характера. это древние эволюционные программы выживания, встроенные в нашу физиологию.</p>
        </section>

        <!-- схема реакции -->
        <section class="reaction-flow">
            <div class="flow-title">биологический цикл реакции</div>
            <div class="flow-grid">
                <div class="flow-card">
                    <div class="flow-step">шаг 01</div>
                    <div class="flow-name">триггер</div>
                    <div class="flow-desc">чтение сообщений, контакт с оценкой, ожидание</div>
                </div>
                <div class="flow-arrow">→</div>
                <div class="flow-card">
                    <div class="flow-step">шаг 02</div>
                    <div class="flow-name">физиология</div>
                    <div class="flow-desc">мгновенно: дыхание, пульс, зажим челюсти</div>
                </div>
                <div class="flow-arrow">→</div>
                <div class="flow-card">
                    <div class="flow-step">шаг 03</div>
                    <div class="flow-name">когниция</div>
                    <div class="flow-desc">с задержкой: мысли, эмоции, объяснения</div>
                </div>
            </div>
        </section>

        <!-- 4 стратегии -->
        <section class="strategies-section">
            <div class="section-label">картотека эволюционных сценариев</div>
            
            <!-- бeй -->
            <div class="strategy-row">
                <div class="strategy-title-box">
                    <div class="strategy-title">бей</div>
                    <div class="strategy-meta">мобилизация</div>
                </div>
                <div class="strategy-details">
                    <div class="detail-item"><strong>в теле:</strong> челюсть крепко сжата, плечи подняты к ушам, внутренний гул, каменная статика.</div>
                    <div class="detail-item"><strong>в языке:</strong> маркеры конкуренции и тотального долга: «я должна», «надо доказать», «во что бы то ни стало».</div>
                </div>
            </div>

            <!-- бeги -->
            <div class="strategy-row">
                <div class="strategy-title-box">
                    <div class="strategy-title">беги</div>
                    <div class="strategy-meta">суета и уход</div>
                </div>
                <div class="strategy-details">
                    <div class="detail-item"><strong>в теле:</strong> потеря опоры под ногами, поверхностное дыхание, хаотичные микро-движения, суета.</div>
                    <div class="detail-item"><strong>в языке:</strong> хаотичная смена тем в разговоре, переключение вкладок, бесконечное «потом разберусь».</div>
                </div>
            </div>

            <!-- зaмри -->
            <div class="strategy-row">
                <div class="strategy-title-box">
                    <div class="strategy-title">замри</div>
                    <div class="strategy-meta">анестезия</div>
                </div>
                <div class="strategy-details">
                    <div class="detail-item"><strong>в теле:</strong> взгляд стекленеет, фоновое онемение, тяжесть в плечах, дыхания почти не слышно.</div>
                    <div class="detail-item"><strong>в языке:</strong> глухие паузы, прокрастинация в ответах на сообщения, дежурные фразы: «мне всё равно», «я не знаю».</div>
                </div>
            </div>

            <!-- угодничeство -->
            <div class="strategy-row">
                <div class="strategy-title-box">
                    <div class="strategy-title">угодничество</div>
                    <div class="strategy-meta">fawn · подстройка</div>
                </div>
                <div class="strategy-details">
                    <div class="detail-item"><strong>в теле:</strong> корпус наклонен вперед, дежурная улыбка (до боли в мышцах лица), холодеющие конечности.</div>
                    <div class="detail-item"><strong>в языке:</strong> режим идеального собеседника, заполнение любой паузы, угодничество: «как вам удобно».</div>
                </div>
            </div>
        </section>

        <!-- цена стратегии -->
        <section class="price-box">
            <div class="price-title">цена автоматической защиты</div>
            <ul class="price-list">
                <li>странная, тяжелая усталость сразу после контакта.</li>
                <li>фоновое раздражение на собеседника после формально «хорошей» встречи.</li>
                <li>ощущение себя выжатой из-за создания искусственной химии.</li>
            </ul>
        </section>

        <!-- вопросы для наблюдения -->
        <section class="questions-box">
            <div class="section-label">вопросы для тихого наблюдения</div>
            <div class="question-item">
                <div class="question-num">01</div>
                <div class="question-text">какая из стратегий включается у меня чаще всего при малейшем дискомфорте?</div>
            </div>
            <div class="question-item">
                <div class="question-num">02</div>
                <div class="question-text">в какой момент удалось поймать физический импульс тела до того, как включились мысли и объяснения?</div>
            </div>
            <div class="question-item">
                <div class="question-num">03</div>
                <div class="question-text">что меняется, если перестать ругать себя за лень или раздражение, признав их биологической защитой тела?</div>
            </div>
        </section>

        <!-- практика недели -->
        <section class="practice-container">
            <div class="practice-title">аудио-практика заземления</div>
            <div class="practice-focus">фокус: опора и выдох · без подготовки · в любой момент дня</div>
            
            <div class="audio-player">
                <button class="play-btn" id="playBtn" onclick="togglePlay()">
                    <div class="play-icon"></div>
                </button>
                <div class="progress-container" id="progressContainer" onclick="setProgress(event)">
                    <div class="progress-bar" id="progressBar"></div>
                </div>
                <span class="time-display" id="timeDisplay">0:00</span>
            </div>
            <p class="manifesto-sub" style="margin-top: 10px;">отклониться назад. отпустить дежурную улыбку. вернуться в свое тело.</p>
        </section>

        <!-- подвал -->
        <footer>
            <div class="footer-quote">первый шаг — не изменить. первый шаг — заметить.</div>
            <div class="footer-meta">kintsugi* · неделя 2 завершена</div>
        </footer>
    </div>

    <!-- симуляция легкого аудиоплеера для красивого отклика -->
    <script>
        let isPlaying = false;
        let progress = 0;
        let interval;
        const playBtn = document.getElementById('playBtn');
        const progressBar = document.getElementById('progressBar');
        const timeDisplay = document.getElementById('timeDisplay');
        const totalDuration = 180; // 3 минуты

        function togglePlay() {
            isPlaying = !isPlaying;
            if (isPlaying) {
                playBtn.classList.add('playing');
                interval = setInterval(() => {
                    progress += (1 / totalDuration) * 100;
                    if (progress >= 100) {
                        progress = 100;
                        togglePlay();
                    }
                    updateUI();
                }, 1000);
            } else {
                playBtn.classList.remove('playing');
                clearInterval(interval);
            }
        }

        function updateUI() {
            progressBar.style.width = progress + '%';
            const currentSeconds = Math.floor((progress / 100) * totalDuration);
            const minutes = Math.floor(currentSeconds / 60);
            const seconds = currentSeconds % 60;
            timeDisplay.innerText = `${minutes}:${seconds < 10 ? '0' : ''}${seconds}`;
        }

        function setProgress(event) {
            const container = document.getElementById('progressContainer');
            const rect = container.getBoundingClientRect();
            const clickX = event.clientX - rect.left;
            const width = rect.width;
            progress = (clickX / width) * 100;
            updateUI();
        }
    </script>
</body>
</html>

```
